package com.company;


import javafx.scene.Node;
//import org.w3c.dom.Node;

public class Main {

    public static void main(String[] args) {
        Node node = null;
        org.w3c.dom.Node anotherNode = null;
    }
    // reasons to use packages
    // programmers can easily determine that classes are related
    // easy to find the classes and interfaces that can provide the functions provided by the package
    // package creates a namespace, class and interface name conflicts are avoided
    // classes in the package can have unrestricted access to one another while still restricting access for classers ouitside the package

    // java.util.Scanner
    // java.util.ArrayList

    // impossible to create a program without using packages

    //java.lang.Integer is where integer is imported (but automatically in base)

    //


}
